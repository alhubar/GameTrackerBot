node_modules/
.env
# Any other env file too — a second one for a test server holds a second real bot token, and
# `.env` alone would not have caught `.env.test`. The example file is the one deliberate exception.
.env.*
!.env.example
data/
npm-debug.log*
*.log
# test/ is committed on purpose: CI runs the suite on every push, which is the only way a
# broken achievement evaluator gets caught before it reaches the server. Note the tests do
# state achievement thresholds, so they are a spoiler README deliberately is not.
# Local-only preview tools that post to a real Discord server. scripts/preflight-upgrade.js is
# shipped, since it is read-only and useful to anyone upgrading an existing database.
scripts/demo-*.js
# Working draft for the GitHub Release body, not a repo document.
CHANGELOG.md
livedbdatacopy/
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.backup-*
# systemd unit for the live host — contains machine-specific paths and the service account.
gametracker.service
CLAUDE.md